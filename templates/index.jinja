<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AI Image Predictor</title>
    <link href="{{ url_for('static', filename='output.css') }}" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet" />
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
      .loader-spin {
        animation: spin 1s linear infinite;
      }
      @keyframes spin {
        from {
          transform: rotate(0deg);
        }
        to {
          transform: rotate(360deg);
        }
      }
    </style>
  </head>
  <body class="bg-gradient-to-br from-warna1 via-warna3 to-white min-h-screen flex items-center justify-center p-4">
    <div id="loading-overlay" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-50 hidden flex flex-col items-center justify-center transition-opacity duration-300">
      <div class="bg-white p-6 rounded-2xl shadow-2xl flex flex-col items-center">
        <svg class="w-12 h-12 text-warna3 loader-spin mb-4" fill="none" viewBox="0 0 24 24">
          <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
          <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
        </svg>
        <h3 class="text-lg font-bold text-gray-800">Sedang Menganalisis...</h3>
        <p class="text-sm text-gray-500">Model AI sedang bekerja</p>
      </div>
    </div>
    <div class="bg-white/95 backdrop-blur-xl w-full max-w-6xl rounded-2xl shadow-2xl overflow-hidden transform transition-all hover:shadow-3xl duration-300">
      <div class="bg-gradient-to-b from-warna3 to-warna4 p-2 text-center relative overflow-hidden">
        <p class="text-2xl font-bold text-white tracking-tight">Image AI Classifier</p>
        <p class="text-slate-300 text-sm mt-1">Klasifikasi Gambar AI Resnet 101</p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 min-h-[465px]">
        <div class="flex flex-col border-r border-gray-100 bg-white relative">
          <div class="p-8 flex-grow flex flex-col justify-center">
            <form id="upload-form" method="POST" enctype="multipart/form-data" class="space-y-6">
              <div class="relative group">
                <label for="file-upload" class="flex flex-col items-center justify-center w-full h-56 border-2 border-dashed border-gray-300 rounded-xl cursor-pointer bg-gray-50 hover:bg-gray-100 hover:border-warna3 transition-all duration-300 group-hover:shadow-sm">
                  <div class="flex flex-col items-center justify-center pt-5 pb-6 px-4 text-center">
                    <svg class="w-12 h-12 mb-3 text-gray-400 group-hover:text-warna3 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
                    </svg>
                    <p class="mb-2 text-sm text-gray-500 group-hover:text-warna4 font-medium">
                      <span class="font-bold">Klik area ini</span> untuk upload gambar
                    </p>
                    <p class="text-xs text-gray-400" id="file-name">PNG, JPG, JPEG (Maks. 5MB)</p>
                  </div>
                  <input id="file-upload" name="file" type="file" class="hidden" required onchange="updateFileName(this)" />
                </label>
              </div>

              <button type="submit" class="w-full text-white bg-gradient-to-r from-warna3 to-warna4 hover:brightness-110 focus:ring-4 focus:ring-gray-200 cursor-pointer font-semibold rounded-lg text-lg px-5 py-3.5 text-center shadow-lg transform transition active:scale-95 duration-200">🔍 Mulai Analisis</button>
            </form>
          </div>
        </div>

        <div class="bg-gray-50/50 p-8 flex flex-col items-center justify-center text-center relative">
          {% if img_path %}
            <div class="animate-fade-in-up w-full">
              <h3 class="text-xs font-bold text-gray-400 uppercase tracking-widest mb-4">Hasil Analisis AI</h3>

              <div class="relative inline-block rounded-lg overflow-hidden shadow-lg ring-4 ring-white mb-6 bg-white">
                <img src="{{ img_path }}" alt="Uploaded Image" class="max-h-64 object-contain mx-auto transform hover:scale-105 transition duration-500" />
              </div>

              <div class="bg-white rounded-xl p-6 border-l-4 border-warna3 shadow-sm">
                <p class="text-sm text-gray-500 mb-2 font-medium uppercase">Hasil Predksi:</p>
                <p class="text-xl font-extrabold text-warna2 tracking-tight">{{ prediction }}</p>
              </div>
            </div>
          {% else %}
            <div class="animate-pulse-slow max-w-xs">
              <div class="mx-auto w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mb-6 text-gray-300">
                <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
              </div>

              <h2 class="text-2xl font-bold text-warna2 mb-3">Menunggu Input...</h2>
              <p class="text-gray-500 leading-relaxed">Silakan pilih gambar di panel sebelah kiri. Sistem kami siap memprediksi gambar AI atau bukan! ✨</p>
            </div>
          {% endif %}
        </div>
      </div>
    </div>

    <script>
      function updateFileName(input) {
        const fileNameDisplay = document.getElementById('file-name')
        const iconSvg = input.parentElement.querySelector('svg')
      
        if (input.files && input.files.length > 0) {
          fileNameDisplay.textContent = 'Siap: ' + input.files[0].name
          fileNameDisplay.classList.remove('text-gray-400')
          fileNameDisplay.classList.add('text-warna3', 'font-bold', 'bg-white', 'px-2', 'rounded')
      
          if (iconSvg) iconSvg.classList.add('text-warna3')
        }
      }
      
      const form = document.getElementById('upload-form')
      const loader = document.getElementById('loading-overlay')
      
      form.addEventListener('submit', function (e) {
        const fileInput = document.getElementById('file-upload')
        if (fileInput.files.length > 0) {
          loader.classList.remove('hidden')
        }
      })
    </script>

    <style>
      @keyframes fade-in-up {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      .animate-fade-in-up {
        animation: fade-in-up 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }
      
      .animate-pulse-slow {
        animation: pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
      }
      @keyframes pulse {
        0%,
        100% {
          opacity: 1;
        }
        50% {
          opacity: 0.7;
        }
      }
    </style>
  </body>
</html>
